{
  "name": "fresh-trace-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "echo \"No separate server dev process yet; server/ currently only contains Prisma schema and seed.\""
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "@supabase/supabase-js": "^2.99.3"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
