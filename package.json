{
  "name": "fresh-trace-root",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:server": "echo \"No separate server dev process yet; server/ currently only contains Prisma schema, seed, and utility scripts.\"",
    "bootstrap:admin": "node ./server/scripts/bootstrap-admin.mjs",
    "cleanup:inventory": "node ./server/scripts/cleanup-inventory-test-data.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "@supabase/supabase-js": "^2.99.3"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "jest": "^30.3.0",
    "prisma": "^6.12.0",
    "ts-jest": "^29.4.9"
  }
}
